# BUILD_MANIFEST.txt template
#
# Copy this file to BUILD_MANIFEST.txt (already gitignored) and let the
# commands in the build guide append to it as you go. Keeping this evidence
# is what lets you prove later whether the packaged station changed between
# UAT and event deployment (see README "Reproducibility" section).
#
# Expected entries, appended in this order as you complete each build step:
#
# OpenVoice commit: <git rev-parse HEAD from upstream/OpenVoice>
# <git remote -v output for OpenVoice>
# MeloTTS commit: <git rev-parse HEAD from upstream/MeloTTS>
# Build freeze date: <date>
# macOS: <sw_vers -productVersion>
# Architecture: <uname -m>
# FFmpeg: <ffmpeg -version | head -1>
# Python: <python --version>
# PyTorch: <python -c 'import torch; print(torch.__version__)'>
# Docker Desktop: <docker version>
# Docker Compose: <docker compose version>
